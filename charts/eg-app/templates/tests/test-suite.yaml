---
# ============================================================
# 🧪 Test 1: Liveness endpoint  (path driven from values)
# ============================================================
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "eg-app.fullname" . }}-test-liveness
  namespace: {{ required "environment.namespace must be set" .Values.environment.namespace }}
  labels:
    {{- include "eg-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  serviceAccountName: {{ .Values.security.serviceAccount.name }}
  containers:
    - name: test-liveness
      image: curlimages/curl:8.6.0
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        runAsNonRoot: true
        runAsUser: 1000
        capabilities:
          drop: ["ALL"]
      command:
        - sh
        - -c
        - |
          SVC="{{ include "eg-app.fullname" . }}.{{ .Values.environment.namespace }}.svc.cluster.local:{{ .Values.networking.service.port }}{{ .Values.application.probes.liveness.path }}"
          echo "==> Liveness target: http://$SVC"

          PASS=0
          for i in $(seq 1 12); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 "http://$SVC")
            echo "  Attempt $i — HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then
              PASS=1
              break
            fi
            sleep 5
          done

          if [ $PASS -eq 1 ]; then
            echo "PASS: Liveness OK"
          else
            echo "FAIL: Liveness did not return 200 after 12 attempts"
            exit 1
          fi

---
# ============================================================
# 🧪 Test 2: Readiness endpoint  (path driven from values)
# ============================================================
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "eg-app.fullname" . }}-test-readiness
  namespace: {{ .Values.environment.namespace }}
  labels:
    {{- include "eg-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "2"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  serviceAccountName: {{ .Values.security.serviceAccount.name }}
  containers:
    - name: test-readiness
      image: curlimages/curl:8.6.0
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        runAsNonRoot: true
        runAsUser: 1000
        capabilities:
          drop: ["ALL"]
      command:
        - sh
        - -c
        - |
          SVC="{{ include "eg-app.fullname" . }}.{{ .Values.environment.namespace }}.svc.cluster.local:{{ .Values.networking.service.port }}{{ .Values.application.probes.readiness.path }}"
          echo "==> Readiness target: http://$SVC"

          PASS=0
          for i in $(seq 1 12); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 "http://$SVC")
            echo "  Attempt $i — HTTP $STATUS"
            if [ "$STATUS" = "200" ]; then
              PASS=1
              break
            fi
            sleep 5
          done

          if [ $PASS -eq 1 ]; then
            echo "PASS: Readiness OK"
          else
            echo "FAIL: Readiness did not return 200 after 12 attempts"
            exit 1
          fi

---
# ============================================================
# 🧪 Test 3: Secret injection  (declared keys only — VITE_STRAPI_URL, VITE_STRAPI_TOKEN)
# ============================================================
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "eg-app.fullname" . }}-test-secrets
  namespace: {{ .Values.environment.namespace }}
  labels:
    {{- include "eg-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "3"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  serviceAccountName: {{ .Values.security.serviceAccount.name }}
  containers:
    - name: test-secrets
      image: busybox:1.36
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        runAsNonRoot: true
        runAsUser: 1000
        capabilities:
          drop: ["ALL"]
      envFrom:
        {{- range .Values.security.envFromSecrets }}
        - secretRef:
            name: {{ .secretName }}
        {{- end }}
      command:
        - sh
        - -c
        - |
          echo "==> Testing secret injection (declared keys only)..."
          FAIL=0

          {{- range .Values.security.externalSecrets.secrets }}
          {{- range .data }}
          VAL=$(printenv {{ .secretKey }} || true)
          if [ -z "$VAL" ]; then
            echo "FAIL: {{ .secretKey }} is missing or empty"
            FAIL=1
          else
            echo "PASS: {{ .secretKey }} is populated"
          fi
          {{- end }}
          {{- end }}

          exit $FAIL

---
# ============================================================
# 🧪 Test 4: DNS resolution
# ============================================================
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "eg-app.fullname" . }}-test-dns
  namespace: {{ .Values.environment.namespace }}
  labels:
    {{- include "eg-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "4"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  serviceAccountName: {{ .Values.security.serviceAccount.name }}
  containers:
    - name: test-dns
      image: busybox:1.36
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        runAsNonRoot: true
        runAsUser: 1000
        capabilities:
          drop: ["ALL"]
      command:
        - sh
        - -c
        - |
          FQDN="{{ include "eg-app.fullname" . }}.{{ .Values.environment.namespace }}.svc.cluster.local"
          echo "==> Testing DNS resolution for $FQDN"

          if ! nslookup "$FQDN"; then
            echo "FAIL: DNS resolution failed for $FQDN"
            exit 1
          fi

          echo "PASS: DNS OK"

---
# ============================================================
# 🧪 Test 5: TCP connectivity  (busybox-safe nc, no -v flag)
# ============================================================
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "eg-app.fullname" . }}-test-network
  namespace: {{ .Values.environment.namespace }}
  labels:
    {{- include "eg-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  serviceAccountName: {{ .Values.security.serviceAccount.name }}
  containers:
    - name: test-network
      image: busybox:1.36
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        runAsNonRoot: true
        runAsUser: 1000
        capabilities:
          drop: ["ALL"]
      command:
        - sh
        - -c
        - |
          HOST="{{ include "eg-app.fullname" . }}.{{ .Values.environment.namespace }}.svc.cluster.local"
          PORT="{{ .Values.networking.service.port }}"
          echo "==> Testing TCP connectivity to $HOST:$PORT"

          if ! nc -z "$HOST" "$PORT"; then
            echo "FAIL: Cannot reach $HOST:$PORT"
            exit 1
          fi

          echo "PASS: TCP connectivity OK"

---
# ============================================================
# 🧪 Test 6: Replica floor  (HPA minReplicas=2, maxReplicas=4)
# ============================================================
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "eg-app.fullname" . }}-test-replicas
  namespace: {{ .Values.environment.namespace }}
  labels:
    {{- include "eg-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "6"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  serviceAccountName: {{ .Values.security.serviceAccount.name }}
  containers:
    - name: test-replicas
      image: curlimages/curl:8.6.0
      securityContext:
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        runAsNonRoot: true
        runAsUser: 1000
        capabilities:
          drop: ["ALL"]
      env:
        - name: MIN_REPLICAS
          value: "{{ .Values.platform.hpa.minReplicas }}"
        - name: MAX_REPLICAS
          value: "{{ .Values.platform.hpa.maxReplicas }}"
        - name: NAMESPACE
          value: "{{ .Values.environment.namespace }}"
        - name: DEPLOY_NAME
          value: "{{ include "eg-app.fullname" . }}"
      command:
        - sh
        - -c
        - |
          echo "==> Checking replica count via Kubernetes API..."
          echo "    Expected: min=$MIN_REPLICAS  max=$MAX_REPLICAS"

          TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
          CA="/var/run/secrets/kubernetes.io/serviceaccount/ca.crt"
          API="https://kubernetes.default.svc"

          READY=$(curl -s --cacert "$CA" \
            -H "Authorization: Bearer $TOKEN" \
            "$API/apis/apps/v1/namespaces/$NAMESPACE/deployments/$DEPLOY_NAME" \
            | grep -o '"readyReplicas":[0-9]*' | grep -o '[0-9]*')

          echo "  Ready replicas: $READY"

          if [ -z "$READY" ]; then
            echo "FAIL: Could not read readyReplicas"
            exit 1
          fi

          if [ "$READY" -lt "$MIN_REPLICAS" ]; then
            echo "FAIL: $READY ready replicas is below HPA minimum of $MIN_REPLICAS"
            exit 1
          fi

          if [ "$READY" -gt "$MAX_REPLICAS" ]; then
            echo "FAIL: $READY ready replicas exceeds HPA maximum of $MAX_REPLICAS"
            exit 1
          fi

          echo "PASS: Replica count $READY is within [$MIN_REPLICAS, $MAX_REPLICAS]"