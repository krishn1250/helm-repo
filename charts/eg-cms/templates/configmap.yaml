apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "eg-strapi.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "eg-strapi.labels" . | nindent 4 }}
data:
  APP_ENV: {{ .Values.namespace | quote }}
  GCP_REGION: {{ .Values.global.region | quote }}
